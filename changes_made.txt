# changes made to run only in level 2 dungeon
1. craftax/craftax_coop/game_logic.py 3551
# disable floor change
2. craftax/craftax_coop/world_gen/world_gen.py 616 
player_level=jnp.asarray(2, dtype=jnp.int32),
3. craftax/craftax_coop/world_gen/world_gen.py 528 
... (x[0], x[1], y[0], y[1], y[2], x[2], x[3], x[4], x[5]), axis=0 ... # swap the level 1 and 2 so that level 2 is vanilla dungeon

# changes made to form subclasses containing 1 each of the specializations (miner, warrior, forager) 
1.craftax/craftax_coop/constants.py 216-218 
class Subclass(Enum):
    A = 0
    B = 1
2. craftax/craftax_coop/craftax_state.py 159
    player_count: int = 6
3. craftax/craftax_coop/world_gen/world_gen.py 509-511
    # Fix player subclasses
    player_sc_order = jnp.array([Subclass.A.value, Subclass.B.value])
    player_sc = player_sc_order[jnp.arange(static_params.player_count) % 2]
4. craftax/craftax_coop/craftax_state.py 82
    player_sc: jnp.ndarray # subclasses
5. craftax/craftax_coop/world_gen/world_gen.py 641
        player_sc = player_sc,

# changes made to ensure only revive players in the same subclass, damage players in other subclasses
1. craftax/craftax_coop/game_logic.py 10-11
    in_same_sc = (jnp.expand_dims(state.player_sc, axis=1) == jnp.expand_dims(state.player_sc, axis=0)).squeeze(axis=2).T
    in_diff_sc = jnp.logical_not(in_same_sc)
2. craftax/craftax_coop/game_logic.py 13-44
... # differentiate same/diff_sc interaction and being interacted with ...

# changes made so that trades only occur among agents in the same subclass
1. craftax/craftax_coop/game_logic.py 3384
    in_same_sc = (jnp.expand_dims(state.player_sc, axis=1) == jnp.expand_dims(state.player_sc, axis=0)).squeeze(axis=2).T
2. craftax/craftax_coop/game_logic.py 3394
        in_same_sc[jnp.arange(static_params.player_count), player_trading_to] # same-sc check in giving condition


